machine:
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - sudo apt-get install -y ifupdown libpango-1.0-0 libpangocairo-1.0-0 libpangoft2-1.0-0 libgvc6 graphviz

test:
  override:
    - echo dotコマンド走らせる(dot -T png server-architecture.dot -o server-architecture.png)
  post:
    - echo hubコマンドでPR出す
