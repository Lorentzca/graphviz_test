machine:
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - sudo apt-get install -y graphviz

test:
  override:
    - echo dotコマンド走らせる(dot -T png server-architecture.dot -o server-architecture.png)
  post:
    - echo hubコマンドでPR出す
